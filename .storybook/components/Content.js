import React from "react";

const Content = ({ children }) => (
  <div
    style={{
      backgroundColor: "#aaddbb",
      width: "100%",
      textAlign: "center"
    }}
  >
    {children}
  </div>
);

export default Content;
